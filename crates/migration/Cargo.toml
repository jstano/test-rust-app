[package]
name = "migration"
version = "0.1.0"
edition = "2024"

[dependencies]
sea-orm-migration = { version = "1", features = ["runtime-tokio-rustls"] }
tokio             = { version = "1", features = ["macros"] }
